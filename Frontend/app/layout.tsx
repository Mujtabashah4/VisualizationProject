import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import { ThemeProvider } from '@/components/theme-provider'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'ViralScope | YouTube Trend Analytics',
  description: 'Real-Time Analytics for 4 Million Trending Videos Across 113 Countries | AI622 Data Science Project',
  generator: 'Next.js',
  authors: [{ name: 'Group-5: Syed Muhammad Mujtaba & Abdul Moeed' }],
  keywords: ['YouTube', 'Viral Prediction', 'Machine Learning', 'Data Analytics', 'Trending Videos'],
  icons: {
    icon: '/icon.svg',
    apple: '/icon.svg',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`font-sans antialiased`}>
        <ThemeProvider
          attribute="class"
          defaultTheme="light"
          enableSystem={false}
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
        <Analytics />
      </body>
    </html>
  )
}
